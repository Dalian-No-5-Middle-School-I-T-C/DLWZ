# 2026-07-21 工作记录

- 修复 `index.html` 3 个 bug（favicon、主题按钮、emerald→红金）。
- 修正 `src/input.css` 中 `primary-800/900` 错误红色值。
- 新增 `editorial.css` 红金精致化纯 CSS 层。
- 成功连接 Ardot 适配器并生成画布：fileId `706312424288038`，含 Header、Hero、和雅教育、荣誉殿堂、校园风光、Footer 六个板块。
- 截图已保存至 `DLWZ/ardot_shots/`。
- 21:30 左右：将画布「校园风光」板块的 4 张 AI/Stock 占位图替换为官网原图：`鸟瞰全景.jpg`、`新教学楼外景.jpg`、`校园活动.JPG`、`毕业典礼.jpg`。
- 21:32：将 `新教学楼外景.jpg` 应用到画布 Hero 首屏主图（节点 `2:24`），替代原 Stock 占位图。
- 21:37：官网动画升级 —— 新增顶部滚动进度条（`#scroll-progress` + `style.css` + `index.js` rAF 驱动）；入场动画改用更顺滑缓动并加 `will-change`；卡片悬浮阴影与渐变叠加由蓝改红金品牌色；新增 `.float-on-hover` 与 `#header.scrolled` 收缩；补充 `prefers-reduced-motion` 无障碍支持。
- 21:45：修复底部地图插件 —— 原百度 `map.baidu.com/search/` 嵌入只显示搜索页/App 遮罩且 `invert(92%)` 滤镜把地图颜色翻坏；改用高德 `uri.amap.com/marker`（坐标 121.59133,38.934013，腾讯地图校核），新增 `.map-card` 红金描边卡片样式。`node --check` 通过。
- 21:45：修复办学历史数字不一致 bug —— 正文/计数动画/footer 写死 `67`，而 JS 动态算 `2026-1957=69`；改为 JS 统一计算并同步 `#school-duration`/`#years-text`/`#stat-years`/`#footer-years`（带 id 兜底）。同步更新画布荣誉墙 Stat1 为 69。
- 21:52：排版优化 + 深色模式 + 调试。(1) 排版：section[id] 加 scroll-margin-top:88px 修锚点被吸顶导航遮挡；标题 text-wrap:balance、正文 text-wrap:pretty、body 行高 1.7、::selection 品牌色、:focus-visible 红描边。(2) 深色模式：新增 darkmode.css（`body.dark` 覆盖中性色板、品牌色保持/提亮、含过渡与 color-scheme），index.html 链接于 editorial.css 后；index.js 顶部加 initTheme（系统偏好 + localStorage 持久化），重写 toggleTheme 避免依赖被 Lucide 替换后的图标节点（按钮加 id=theme-btn、每次重建图标）。(3) 调试：删除已注释 loader 在 index.js 仍 `getElementById('loader')` 的死代码（load 时抛 null 错误）；`node --check` 通过。
- 22:02：优化深色模式字体可读性 —— 提亮 darkmode.css 文字层级：text-slate-600→#cabdaf、500→#b3a698、400→#9c8e80（正文/说明达 AA）；品牌金色 text-school-gold→#e6c063；深红页脚 text-white/50→rgba(255,255,255,0.72)。
